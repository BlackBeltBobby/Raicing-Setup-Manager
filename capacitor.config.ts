import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.tylersingleton.racingsetup',
  appName: 'Racing Setup Manager',
  // Capacitor serves the Vite production build; run `npm run build` first.
  webDir: 'dist',
}

export default config
